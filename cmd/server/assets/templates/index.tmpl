{{ define "index" }}<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/static/base.css" />
    <link rel="stylesheet" href="/static/theme.css" />
    <link rel="stylesheet" href="/static/styles.css" />
    <script src="/static/htmx.min.js" defer></script>
    <title>{{ .Title }}</title>
</head>
<body>
    <nav class="nav" aria-label="Main navigation">
        <a href="/ui/" class="nav__brand">{{ .AppName }}</a>
        <input type="checkbox" id="nav-toggle" class="nav__toggle" aria-hidden="true" />
        <label for="nav-toggle" class="nav__toggle-label" aria-label="Toggle navigation menu">
            <span class="nav__hamburger"></span>
        </label>
        <div class="nav__links">
            <a href="/ui/" class="nav__link">Home</a>
            <a href="/auth/logout" class="nav__link">Logout</a>
        </div>
    </nav>
    <main>
        <div class="card">
            <div class="card__header">
                <h1>Welcome, {{ .Name }}!</h1>
            </div>
            <div class="card__body">
                <p><strong>Email:</strong> {{ .Email }}</p>
                <p><strong>Issuer:</strong> {{ .Issuer }}</p>
                <p><strong>Session ID:</strong> {{ .SessionID }}</p>
                <p><strong>Subject:</strong> {{ .Subject }}</p>
                <p><strong>Verified:</strong> {{ .Verified }}</p>
            </div>
            <div class="card__footer">
                <a href="/auth/logout" class="btn btn-primary">Logout</a>
            </div>
        </div>
    </main>
    <div class="action-bar" aria-label="Quick actions">
        <a href="/ui/" class="action-bar__item">
            <span aria-hidden="true">🏠</span>
            <span>Home</span>
        </a>
        <a href="/auth/logout" class="action-bar__item">
            <span aria-hidden="true">🚪</span>
            <span>Logout</span>
        </a>
    </div>
</body>
</html>
{{ end }}
